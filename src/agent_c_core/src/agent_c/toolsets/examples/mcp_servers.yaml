# Example MCP server configuration
servers:
  calculator:
    command: python
    args: ["-m", "calculator_server"]
    env:
      API_KEY: "your-api-key"
      
  filemanager:
    command: python
    args: ["~/mcp_servers/filemanager.py"]
    env:
      ROOT_DIR: "/path/to/files"